const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // onde vai ficar esse arq de config
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),// caminhos das minhas migrates
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // ele cria pra gente
}
